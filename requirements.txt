# LottAI -- Pick 3 autoresearch dependencies
# Install: pip install -r requirements.txt

# Core data and ML
numpy>=2.0
pandas>=2.0
scipy>=1.10
scikit-learn>=1.4

# Gradient boosting models
lightgbm>=4.0
xgboost>=2.0

# Environment / config
python-dotenv>=1.0

# Phase 3: Software 2.0 predictor (GPU, CUDA 12.1 wheels)
# Install with: pip install torch==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
pytest>=9.0

# Phase 3 Stage D: TimesFM-3 zero-shot forecasting (CPU inference; see
# phase3/stage_d_timesfm.py docstring). Install with: pip install timesfm[torch]==3.0.1
timesfm[torch]==3.0.1
